class MainClass {
    def main(): int {
        writeln(new SecondMain().main());
        return 0;
    }
}

class Pat {
    var kharoo : int;
}

class SecondMain extends Pat {
    var s : string;
    var arr : int[];
    def oops() : int {
        var x : boolean;
        var z : boolean;
        var y : int;
        #s = "hi";
        kharoo = x && z;
        if ( x > 14) then 
            x = 14;
        return y;
    }
}

class Mat {
    var k : Pat;
}
