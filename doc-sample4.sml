class MainClass {
    def main(): int {
        writeln(new SecondMain().main());
        return 0;
    }
}

class Pat {
    var kharoo : int;
    var kharoo2 : string;
}

class SecondMain extends Pat {
    var s : Mat;
    var ss : string;
    var arr : int[];
    def main() : Pat {
        var x : int;
        var z : int;
        var y : int;
        var h : int;
        var j : Pat;
        #y = kk;
        ss = "hi";
        arr = new int[5];
        arr[4] = 12;
        y = -y;
        kharoo = x * z + y * ss;
        j = s.Poop(z,f);
        if ( x < 14) then 
            x = 14;
        while ( x == pp){
            x = 50;
        }
        return j;
    }
}

class Mat {
    var k : Pat;
    def Poop (x1: int,y1:int) : Pat{
        var poop1 : string;
        var poop2 : int;
        return k;
    }
}
