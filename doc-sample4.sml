class MainClass {
    def main(): int {
        writeln(new SecondMain().main());
        return 0;
    }
}

class Pat {
    var kharoo : int;
}

class SecondMain extends Pat {
    var s : Mat;
    var arr : int[];
    def main() : int {
        var x : int;
        var z : int;
        var y : int;
        var h : int;
        #s = "hi";
        arr = new int[2];
        arr[1] = "hi";
        kharoo = x * z + y * h;
        h = s.Poop();
        if ( x > 14) then 
            x = 14;
        return y;
    }
}

class Mat {
    var k : Pat;
    def Poop () : int{
        var poop1 : string;
        var poop2 : int;
        return poop2;
    }
}
